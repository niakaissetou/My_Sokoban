/*
** EPITECH PROJECT, 2022
** detect_o
** File description:
** in_the_file_you_know
*/
#include "struct.h"
#include <stddef.h>
#include <stdio.h>

/*int detect_o(char **tab, var_t *pos)
{
    int i = 0;
    int j = 0;
    int count = 0;

    printf("test\n");
    for (; tab[i] != NULL; i++) {
        for (; tab[i][j] != '\0'; j++) {
            if (tab[i][j] == 'O') {
                count++;
                printf("count doit ê a deux et il est a = %d\n", count);
            }
        }
        j = 0;
    }
    return count;
}
*/
